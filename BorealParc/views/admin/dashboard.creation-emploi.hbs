<div class="container">
    <div class="row">
        <div class="col s12">
            <a href="/dashboard" class="grey lighten-1 waves-effect waves-light btn"><i class="material-icons left">arrow_back</i>Retour</a>
        </div>
        {{# if success }}
            <div class="center admin-form-ok">
                <h5><i class="material-icons">verified_user</i> L'offre d'emploi a bien été enregistrée</h5>
            </div>
        {{ else }}
            {{# if errors }}
            <div class="center admin-form-wrong">
                <h5><i class="material-icons">close</i> Erreur: l'offre d'emploi n'a pas été enregistrée</h5>
                <ul>
                    {{# each errors }}
                        <li>{{ this.msg }}</li>
                    {{/each}}
                </ul>
            </div>
            {{/if}}
        {{/if}}

        <form action="/dashboard/creation-emploi" method="post" enctype="multipart/form-data" class="col s12">

            <div class="row">
                <div class="input-field col m12 l6">
                    <input id="title" type="text" name="title" class="validate">
                    <label for="title">Titre de l'offre :</label>
                </div>
                <div class="input-field col m12 l6">
                    <input id="description" type="text" name="description" class="validate">
                    <label for="description">Description de l'offre :</label>
                </div>
                <div class="input-field col m12 l6">
                    <input id="description" type="text" name="poste" class="validate">
                    <label for="description">Titre du poste :</label>
                </div>
            </div>
            <div class="row">
                <div class="input-field col s6">
                    <button class="indigo lighten btn waves-effect waves-light" type="submit" name="action">Créer l'offre d'emploi
                    <i class="material-icons right">send</i>
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>
{{> script/basic}}